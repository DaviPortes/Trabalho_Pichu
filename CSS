body {
    font-family: Arial, sans-serif;
    background-color: black;
    color: white;
    text-align: center;
    margin: 0;
    padding: 0;
}

header {
    background-color: rgba(34, 34, 34, 0.9); /* 0.9 referente a transparência */
    width: 100%;
    padding: 15px;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1000; /*trava o cabeçalho no topo */
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 30px;
    box-sizing: border-box;
}

header h1 {
    font-size: 24px;
    margin: 0;
    letter-spacing: 1px;
}

nav ul {
    padding: 0;
    list-style: none;
    display: flex;
    gap: 15px;
    flex-wrap: nowrap; /* alinha itens cabeçaljo */
    justify-content: center;
}

nav ul li {
    display: inline;
}

nav ul li a {
    color: white;
    text-decoration: none;
    font-size: 18px;
    padding: 10px 15px;
    transition: 0.3s;
    white-space: nowrap;
}

nav ul li a:hover {
    color: #ffcc00;
}

.hero {
    margin-top: 100px;
    padding: 80px 20px;
    background: url('imagens/hero.jpeg') no-repeat center;
    background-size: cover;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

.hero h2 {
    font-size: 36px;
    margin-bottom: 10px;
    text-transform: uppercase;
}

.hero p {
    font-size: 18px;
    max-width: 600px;
}

.gallery {
    padding: 60px 20px;
}

.gallery h2 {
    font-size: 28px;
    margin-bottom: 20px;
}

/* CSS Grid */
.image-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 15px;
    justify-items: center;
    max-width: 900px;
    margin: auto;
}

.product {
    text-align: center;
    background-color: #333;
    padding: 20px;
    border-radius: 10px;
    color: white;
    transition: transform 0.3s;
}

.product:hover {
    transform: scale(1.05);
}

.product-name {
    font-size: 18px;
    font-weight: bold;
    margin: 10px 0;
}

.product-price {
    font-size: 16px;
    color: #ffcc00;
    margin-bottom: 15px;
}

.add-to-cart {
    background-color: #ffcc00;
    border: none;
    color: black;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s;
}

.add-to-cart:hover {
    background-color: #ffaa00;
}

/* padroniza imagem produtos */
.product img {
    width: 100%;
    max-width: 280px; /* Largura máxima para as imagens */
    height: 180px;    /* Altura padronizada */
    object-fit: cover; /* impede distorção */
    border-radius: 10px;
}

/* Estilo do botão de carrinho */
.view-cart-button {
    background-color: #ffcc00;
    color: black;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
    font-size: 18px;
    transition: background-color 0.3s;
    margin-left: 20px;
}

.view-cart-button:hover {
    background-color: #ffaa00;
}

.contact {
    padding: 40px;
    background-color: #222;
    height: 60px;
}

.contact h2 {
    font-size: 26px;
}

.contact p {
    font-size: 18px;
    margin-top: 5px;
}

footer {
    background-color: #000;
    padding: 15px;
    text-align: center;
    margin-top: 40px;
}

/* Media Query */
@media (max-width: 768px) {
    header {
        flex-direction: column;
        text-align: center;
    }

    nav ul {
        flex-wrap: nowrap; /* Não deixa o menu no mobile ficar empilhado */
        justify-content: center;
    }
}
